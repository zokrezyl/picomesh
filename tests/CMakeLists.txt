# Future home for engine/runtime smoke tests.
#
# Performance harness for the picoforge scenario: a load generator that
# drives the gateway and reports latency/throughput (target picoforge-perf).
add_subdirectory(performance/picoforge)
