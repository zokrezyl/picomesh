# Future home for engine/runtime smoke tests.
#
# Performance harness for the git-yaafc scenario: a load generator that
# drives the gateway and reports latency/throughput (target yaafc-perf).
add_subdirectory(performance/git-yaafc)
